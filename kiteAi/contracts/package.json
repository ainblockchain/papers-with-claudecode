{
  "name": "learning-ledger-contracts",
  "version": "1.0.0",
  "description": "LearningLedger smart contract for Papers LMS on Kite AI",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network kiteTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network kiteMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
